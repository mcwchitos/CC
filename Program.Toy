class C extends D is
    this (A : Array[Integer], B : Boolean) is end
    
    var arr : Array[Integer]
    
    method func : Integer
    is
        a := this
        while this loop
        end
        
        if i.greater(j) then
        b:=2
        else
        c:=3
        end
        
        return a.b.c(1)
    end
end